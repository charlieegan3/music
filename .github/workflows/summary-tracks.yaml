name: summary-tracks

on:
  schedule:
  - cron: "0 4 * * 2,4,6"

jobs:
  refresh:
    name: run
    runs-on: ubuntu-latest
    steps:

    - name: Download Image
      uses: nick-invision/retry@v1
      with:
        timeout_minutes: 3
        max_attempts: 5
        command: docker pull charlieegan3/music:latest

    - name: Checkout
      uses: actions/checkout@v2

    - name: Run
      env:
        CONFIG_FILE: ${{ secrets.CONFIG_FILE }}
      run: |
        echo $CONFIG_FILE | base64 -d > config.yaml

        docker run -v $(pwd)/config.yaml:/config.yaml charlieegan3/music:latest summarize tracks
